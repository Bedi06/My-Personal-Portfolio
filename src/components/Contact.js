import { Box } from "@mui/material";
export default function Contact() {
  return (
    <Box id="contact" sx={{ height: "60vh", paddingTop: "100px" }}>
      <h2
        style={{
          fontFamily: "monospace",
          display: "flex",
          justifyContent: "center",
          alignItems: "center",
          marginTop: "100px",
        }}
      >
        04.Say Hello
      </h2>
    </Box>
  );
}
